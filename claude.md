# 📜 Project Constitution: claude.md

## 🚨 MANDATORY CONTEXT INITIALIZATION 🚨
**CRITICAL RULE:** Do NOT read through past chat history or blindly search through project files to understand the project state. Doing so wastes tokens and context windows.
**INSTEAD:** You MUST immediately read the `handover.md` file located in the root directory. This document is the single source of truth for the project's state, technology stack, aesthetic goals, and recent changes. Always keep `handover.md` updated after making significant changes, and always keep `gemini.md` and `claude.md` in sync.

## 🏗️ Architectural Invariants
*Core rules that the system must never break.*

## 📊 Data Schema (Payload Shapes)
### Input Schema
```json
{
  "example": "shape"
}
```

### Output Schema
```json
{
  "example": "shape"
}
```

## 🛠️ Maintenance Log
*Operational health and update history.*
